import React from 'react';
import { Head, Main, NextScript } from 'next/document';

export default () => (
  <html lang="ja">
    <Head />
    <body>
      <Main />
      <NextScript />
    </body>
  </html>
);
